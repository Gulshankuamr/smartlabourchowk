import DownloadAppPage from '../../components/app-download/DownloadAppPage'

export const metadata = {
  title: 'Download App',
  description: 'Install Smart Labour Chowk — full access to numbers, chat, apply, and hire.',
  alternates: { canonical: '/download-app' },
}

export default function Page() {
  return <DownloadAppPage />
}
